{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "./image.json",
  "title": "Sikker Selvbetjening Image Config",
  "description": "Schema for a single image configuration.",
  "$ref": "#/$defs/image_entry",
  "$defs": {
    "policy_file_base": {
      "type": "string",
      "pattern": "^(?:\\.\\./policies|policies)/.+\\.ya?ml$",
      "description": "Relative policy file path under policies/... (for example ../policies/bibliotek-selvbetjeningspc.yml).",
      "x-ssb-file": {
        "enabled": true,
        "assetPrefixTemplate": "../policies/"
      }
    },
    "image_entry": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "policies", "image_id"],
      "properties": {
        "name": {
          "type": "string",
          "title": "Image Name",
          "minLength": 1
        },
        "description": {
          "type": "string",
          "title": "Description",
          "description": "Optional description of the image",
          "minLength": 1
        },
        "policies": {
          "type": "array",
          "title": "Policies",
          "description": "List of relative policy file paths assigned to this image.",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/policy_file_base"
          },
          "uniqueItems": true
        },
        "image_id": {
          "type": "string",
          "title": "Image Name",
          "description": "Identifier-safe image key for automation.",
          "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
        }
      }
    }
  }
}

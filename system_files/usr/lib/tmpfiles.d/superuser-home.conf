# Create the superuser home directory under the stateful /var/home volume
# on first boot after a fresh ISO installation.
# Without this, the directory is absent because /var is stateful and not
# seeded from the container image, causing login to fail with
# "Failed to change to directory /var/home/superuser".
d /var/home/superuser 0700 superuser superuser - -
